# Fonction qui vérifie si deux chaînes sont des anagrammes l'une de l'autre (mêmes lettres, dans un ordre différent), sans utiliser la methode sorted(). Pour pimenter les choses

def est_anagramme(string_1: str, string_2: str) -> bool:
    pass